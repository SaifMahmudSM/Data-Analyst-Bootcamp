
1. Ientify the pair of customers who live in same city?

SELECT 
  c.CustomerName, 
  cc.CustomerName
FROM Customers as c
INNER JOIN Customers as cc
  ON c.Country = cc.Country
  AND c.customerID != cc.customerID
;

2. Find the Customers who spent most on their orders?

SELECT 
  CustomerName,
  Sum(price) as Total_spent
FROM Customers as C
INNER JOIN Orders as O
  ON C.CustomerID = O.CustomerID
INNER JOIN Products as P
  ON O.ProductID = P.ProductID
Group By CustomerName
Order BY Sum(price) DESC
Limit 1;

3. Find the Customers who have ordered more than one type of products?

SELECT 
  C.CustomerName
FROM Customers as C
INNER JOIN Orders as O
  ON C.CustomerID = O.CustomerID
Group By C.CustomerName
Having Count(OrderID) > 1;

4. List of all products and their corrospoding orders, including products that have never been order?

SELECT 
  OrderID,
  ProductName,
  ProductName
FROM Products as p
LEFT JOIN Orders as O
  ON p.ProductID = O.ProductID;

5. Retrive all orders placed by customers from the USA

SELECT 
  OrderID
FROM Customers as C
INNER JOIN Orders as O
  ON C.CustomerID = O.CustomerID
  AND Country = 'USA';

6. Find the Cusotmers who have Ordered a product above $500?


SELECT 
  Distinct customerName
FROM Customers as c
LEFT JOIN Orders as o
  ON c. CustomerID = o.CustomerID
LEFT JOIN Products as P
  ON o.ProductID = P.ProductID
WHERE Price > 500;

7. Find the customers who haveed same product more than once?

SELECT Distinct CustomerName 
  FROM   
    (SELECT 
        ProductID,
        CustomerName,
        count(OrderID) as orders
      FROM Customers as c
      INNER JOIN Orders as o
        ON c. CustomerID = o.CustomerID
       Group BY ProductID,CustomerName
       Having count(orderID) > 1 ) 
  Multipule_order
